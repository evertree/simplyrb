<% @posts.each do |post| %>
  <% @post = post %>
  <%= render 'post' %> 
  <%= link_to comment_count, show_post_path(@post.url_title)%>
  <%= link_to 'Add comment', new_comment_path(@post.url_title) %>
<% end %>
<br />

<%= link_to 'New Post', new_post_path %>
