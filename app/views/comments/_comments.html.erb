<% @post.comments.reverse.each do |comment| %>
	<div class="comment">
		<%= commenter_says(comment.commenter) %>
		<div class="time">
			<%= time_ago(comment.created_at) %>
		</div>
		<div class="body"><%= p_strip(simple_format comment.body) %></div>
	</div>
<% end %>